bin/
target/
.factorypath
# .classpath are checked in because otherwise Eclipse doesn't detect the src directories (for currently unknown reasons)
# .project must be checked in because it is used for the linkedResources for umlet-eclipse-plugin
/.metadata/
/.recommenders/
umlet-eclipse-plugin/src/main/java/com/baselet/plugin/swt_awt/
*swp
External Plug-in Libraries/
Umlet_Eclipse_Plugin_Workspace/
